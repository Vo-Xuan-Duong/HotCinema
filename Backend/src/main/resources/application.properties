spring.application.name=hotcinemas_be

# Config database Postgres
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:123456}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

google.client.id=${GOOGLE_CLIENT_ID:your_google_client_id}
google.client.secret=${GOOGLE_CLIENT_SECRET:your_google_client_secret}
# Config for JWT
jwt.secret.access=${JWT_SECRET_ACCESS:hotcinemas_jwt_secret_access_key}
jwt.secret.refresh=${JWT_SECRET_REFRESH:hotcinemas_jwt_secret_refresh_key}
jwt.expiration.access=${JWT_EXPIRATION_ACCESS:86400000}
jwt.expiration.refresh=${JWT_EXPIRATION_REFRESH:604800000}
jwt.issuer=${JWT_ISSUER:hotcinemas}

# Config for Redis
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# Config for CORS
cors_allowed_origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5500,http://127.0.0.1:5500,https://botchily-radiotelegraphic-ezequiel.ngrok-free.dev}

# Swagger/OpenAPI Configuration
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
#springdoc.swagger-ui.operationsSorter=method
#springdoc.swagger-ui.tagsSorter=alpha

#Config for mail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:sendmailtestdev123@gmail.com}
spring.mail.password=${MAIL_PASSWORD:kstvtnhfmvdfdmbs}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Config for Cloudinary
cloudinary.url=${CLOUDINARY_URL:cloudinary://163921533319941:ti_G2Uy6mscaGGVUWtfobFkvIxo@displayvxdhere}

# ===== Request/Response Logging =====
# Show requestId from MDC in log level section
logging.pattern.level=%5p [reqId:%X{requestId}]

# Config MOMO
momo.partnerCode=${MOMO_PARTNER_CODE:MOMONPMB20210629}
momo.partnerName=${MOMO_PARTNER_NAME:HotCinemas}
momo.accessKey=${MOMO_ACCESS_KEY:Q2XhhSdgpKUlQ4Ky}
momo.secretKey=${MOMO_SECRET_KEY:k6B53GQKSjktZGJBK2MyrDa7w9S6RyCf}
momo.endPoint=${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api/create}
momo.redirectUrl=${MOMO_REDIRECT_URL:http://localhost:5173/booking/callback}
momo.ipnUrl=${MOMO_IPN_URL:https://botchily-radiotelegraphic-ezequiel.ngrok-free.dev/api/v1/payments/momo-callback}
momo.requestType=${MOMO_REQUEST_TYPE:captureWallet}